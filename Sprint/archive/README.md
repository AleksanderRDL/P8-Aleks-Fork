# Archived Range-QDS Notes

These files are historical context from the workload-aware `range_aware` phase.
They should not be used as the active redesign reference.

Use them for:

- understanding why `range_aware` became a useful diagnostic and teacher
- comparing future workload-blind results against a workload-aware upper bound
- recovering old benchmark numbers without keeping generated artifacts around

Do not use them for:

- final workload-blind model claims
- current benchmark protocol decisions
- active acceptance criteria
