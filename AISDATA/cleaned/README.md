# Cleaned AIS Data

Place AIS files here after they have had faulty data removed by the cleaning
pipeline.

This folder is for cleaned source data used by later database import or machine
learning steps. Outputs produced by ML experiments should go in a separate
output folder, not here.

Files in this folder are ignored by git because they can be large. Keep this
README tracked so the folder exists in fresh checkouts.
