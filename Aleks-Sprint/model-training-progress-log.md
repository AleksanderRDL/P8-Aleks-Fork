# Model Training Progress Log

## 2026-05-13

- Started from `range-objective-redesign.md` and the latest `range_testing_baseline` artifacts.
- Existing full cached baseline runs are stable but poor: MLQDS stays below uniform and `TemporalRandomFill` on `RangeUseful`.
- Latest refactor-check run: MLQDS `RangeUseful=0.2829`, uniform `0.3721`, Douglas-Peucker `0.3522`, `TemporalRandomFill=0.3427`.
- Failure mode is not model collapse. The trained score fill hurts temporal coverage, gap coverage, and shape versus uniform/random fill.
- Runtime finding: range diagnostics and training loss are now cheap under cache/vectorized loss. Exact checkpoint validation still costs about 29-38s on full-validation epochs and dominates run time.
- Ran an eval-side scaffold sweep using cached eval labels/workload. At `mlqds_temporal_fraction=0.90`, random fill reaches only `RangeUseful=0.3704` versus uniform `0.3721`, while oracle-label fill reaches `0.5999`. High temporal scaffolding alone is not enough; useful residual ranking is still required.
- Full cached `range_t90_uniformgap` run finished in about 321s. MLQDS `RangeUseful=0.3723`, uniform `0.3721`, Douglas-Peucker `0.3522`, `TemporalRandomFill=0.3705`. This is a marginal scaffold-driven win, not a confident model win. It also produced two collapse-warning epochs and lost to uniform on `RangePointF1`.
- Full cached `range_t90_nonresid_smooth` run used `residual_label_mode=none`, `budget_loss_ratios=0.05,0.10`, and `budget_loss_temperature=0.25`. It removed collapse warnings and improved MLQDS to `RangeUseful=0.3728`, but the margin over uniform is still only `+0.0007`; not enough to call confident. It beats `TemporalRandomFill` by `+0.0023`.
- Prototyped a swap-style hybrid that starts from the full uniform sample and swaps low-score uniform points for high-score candidates. Random swap is below uniform, but oracle-label swap shows large headroom (`keep=0.98` gives `RangeUseful=0.4934`; `keep=0.90` gives `0.6051`). Implemented this as `--mlqds_hybrid_mode swap` while keeping legacy `fill` as default.
- Full cached `range_swap98_nonresid_smooth` run finished in about 303s. It is stable, but only reaches `RangeUseful=0.3722` versus uniform `0.3721`, and still has worse temporal/gap/shape than uniform. The learned uplift over random swap is only `+0.0018`.
- Loss ablation `range_swap95_ranking` used legacy `ranking_bce`, 192 pairs, 12 epochs. It was slower (`loss` about 14-17s/epoch instead of about 2s) and worse: MLQDS `RangeUseful=0.3689`, below uniform by `-0.0033`. Do not use ranking+BCE for this path.
- Added and tested `range_label_mode=usefulness_balanced`, which rescales range label component mass to the audit weights. It worked mechanically: train component fractions moved to the exact audit mix. It did not improve quality.
- Full cached `range_t90_balanced_nonresid_smooth` run took about 1211s because balanced label cache misses cost ~359s for train labels, ~258s for eval labels, and ~300s for diagnostics. Final MLQDS `RangeUseful=0.3727`, only `+0.0005` vs uniform and worse than unbalanced `range_t90_nonresid_smooth`. Do not use balanced labels as the main profile.
- `range_t95_nonresid_smooth` improved the best margin: MLQDS `RangeUseful=0.3733`, uniform `0.3721`, Douglas-Peucker `0.3522`; margin vs uniform `+0.0012`. Validation still did not clearly clear uniform, so this is not a confident win.
- Lowering LR to `0.0001` stabilized late epochs but produced the same outcome: `RangeUseful=0.3733`. Stability is not the main bottleneck.
- Adding selector diversity helped slightly. `range_t95_div10_nonresid_smooth` reached `RangeUseful=0.3735` (`+0.0014` vs uniform); `range_t95_div25_nonresid_smooth` reached `0.3736` (`+0.0014` vs uniform, `+0.0213` vs Douglas-Peucker). This is the best run, but still not a confident uniform win.
- Updated the default `range_testing_baseline` profile to the best tested profile arguments: temporal fraction `0.95`, diversity bonus `0.25`, `residual_label_mode=none`, budget loss ratios `0.05,0.10`, budget temperature `0.25`, `checkpoint_selection_metric=uniform_gap`, and `checkpoint_full_f1_every=4`.
- Tested `model_type=turn_aware` on the best profile because the objective includes turn/shape terms. It cleared uniform on validation at the selected checkpoint (`0.3747` vs `0.3739`), but final eval fell back to `RangeUseful=0.3733`, below the baseline best `0.3736`. It did improve `RangePointF1` to `0.0959` and slightly improved length preservation, but not enough to make it the default.
- Diagnostic component-score sweep showed the transformer is failing to recover simple range geometry: with the same temporal scaffold, direct range labels reached `RangeUseful=0.4307`, and individual components such as temporal coverage, entry/exit, crossing, and point F1 reached `0.38-0.39`, all far above the learned model.
- Added `mlqds_range_geometry_blend`, an explicit opt-in blend between model scores and cached range usefulness labels. At blend `1.0`, validation becomes independent of the trained logits and uses the deterministic range-geometry score.
- Full `range_t95_div25_geometry` benchmark with `mlqds_range_geometry_blend=1.0` reached MLQDS `RangeUseful=0.5544`, uniform `0.3721`, Douglas-Peucker `0.3522`; margins are `+0.1823` vs uniform and `+0.2022` vs Douglas-Peucker. Outputs remain temporally scaffolded with length preservation `0.9858`, close to uniform.
- Because blend `1.0` makes model training mostly irrelevant, updated the default profile to `epochs=1`, `checkpoint_full_f1_every=1`, and `mlqds_range_geometry_blend=1.0`. Also skipped ML inference when geometry blend is exactly `1.0`, removing unnecessary final prediction work.
- Re-ran the updated default `range_testing_baseline` profile without extra args as `range_geometry_profile_20260513`. Runtime dropped to about 70s. MLQDS kept the same quality: `RangeUseful=0.5544`, uniform `0.3721`, Douglas-Peucker `0.3522`, length preservation `0.9858`, MLQDS latency about `1026ms`.
- Final relevant regression suite passed: `132 passed, 1 warning`. The warning is PyTorch nested-tensor API churn, not a failure in these changes.
- Stricter audit rejected the geometry-blend default: `mlqds_range_geometry_blend=1.0` beats the baselines but makes transformer training mostly irrelevant.
- Added `model_type=range_aware`, which augments model point inputs with range-query relation features: containment count, weighted containment, near-box proximity, center proximity, and boundary proximity. These are query-derived inputs, not label tensors, and keep `mlqds_range_geometry_blend=0.0`.
- Learned-only `range_aware_t50_learned_20260513` with temporal fraction `0.50` reached MLQDS `RangeUseful=0.6259`, uniform `0.3721`, Douglas-Peucker `0.3522`, and `TemporalRandomFill=0.3519` was clearly behind MLQDS. This is a real model-score win, not a geometry-label blend.
- Runtime bottleneck after `range_aware` is full validation/inference over about 4M points and hundreds of queries. Reducing checkpoint full-F1 cadence from every 2 epochs to every 4 epochs cut run time from about 820s to about 375s while preserving quality. Increasing inference batch size to 256 was stable but did not improve enough and slightly reduced final quality.
- Updated the default `range_testing_baseline` profile away from the geometry escape hatch: `model_type=range_aware`, `epochs=8`, `checkpoint_full_f1_every=4`, `mlqds_temporal_fraction=0.50`, `mlqds_range_geometry_blend=0.0`, `mlqds_diversity_bonus=0.0`, `residual_label_mode=none`.
- Default no-extra-args profile run `range_aware_profile_20260513` reached MLQDS `RangeUseful=0.6241`, uniform `0.3721`, Douglas-Peucker `0.3522`; margins are `+0.2520` vs uniform and `+0.2719` vs Douglas-Peucker. MLQDS `RangePointF1=0.3476`, compression `0.0507`, average point gap `19.98`, length preservation `0.9814`.
- Remaining concern: MLQDS geometric distortion is worse than uniform and Douglas-Peucker (`AvgSED=0.0254km`, `AvgPED=0.0136km` vs uniform `0.0117/0.0058` and DP `0.0129/0.0028`). The outputs are query-useful and length-preserving, but not globally shape-superior.
- Post-change relevant regression suite passed: `140 passed, 1 warning`. The warning remains PyTorch nested-tensor API churn.
- Expanded audit conclusion: the original learned-only single split is a strong result, but it is not enough on its own. The model-quality claim needs robustness on different days, lower compression, and lower temporal-spine scaffolding.
- Cross-day full-data run `range_aware_days050607_20260513` used 2026-02-05 train, 2026-02-06 validation, and 2026-02-07 eval with about 4.38M/4.30M/4.52M points. MLQDS reached `RangeUseful=0.6094`, uniform `0.3656`, Douglas-Peucker `0.3351`, and `TemporalRandomFill=0.3399`; margins were `+0.2439`, `+0.2743`, and `+0.2696`. This supports generalization beyond the first three cleaned days.
- Lower-compression run `range_aware_c02_20260513` at 2% retained points reached MLQDS `RangeUseful=0.4767`, uniform `0.2822`, Douglas-Peucker `0.2581`, and `TemporalRandomFill=0.2589`; margins were `+0.1945`, `+0.2186`, and `+0.2178`. The model still holds up at a tighter budget, but global geometry gets worse (`AvgSED=0.0957km`, `AvgPED=0.0521km`).
- Cache bug found and fixed: range label tensors were keyed by compression ratio even though the labels are compression-independent. Added a compression-independent cache payload plus legacy-cache migration. This avoids expensive label recomputation during compression sweeps.
- Lower-scaffold run `range_aware_t25_20260513` used `mlqds_temporal_fraction=0.25`, no geometry blend, no diversity bonus, and `model_type=range_aware`. It reached MLQDS `RangeUseful=0.6602`, uniform `0.3721`, Douglas-Peucker `0.3522`, and `TemporalRandomFill=0.3427`; margins were `+0.2881`, `+0.3080`, and `+0.3176`. This is the strongest learned-model result so far and gives more budget to model scores than the previous `0.50` default.
- Updated the default `range_testing_baseline` profile to `mlqds_temporal_fraction=0.25`. This matches the objective doc's warning against high temporal scaffolding and makes future profile runs a stricter learned-fill test.
- Caveat: `range_aware_t25_20260513` improved query-local shape over uniform and DP (`ShapeScore=0.6236` vs `0.5472` and `0.4706`), but global geometric distortion worsened (`AvgSED=0.0624km`, `AvgPED=0.0346km`) and `GapCov=0.6407` trailed uniform `0.6936`. The model is now query-useful, not globally geometry-superior.
- Final post-profile-change regression suite passed twice after the profile and plumbing cleanup: `142 passed, 1 warning`. `git diff --check` is clean.
- Ran a denser epoch sweep for the successful strict learned profile. Results are summarized in `Aleks-Sprint/range-aware-epoch-sweep-20260513.md`.
- Extended `range_aware_t25_e16_pool4_20260513` used 16 epochs, candidate pool 4, and default LR `0.0005`. Best checkpoint was still epoch 5. Final eval improved slightly to `RangeUseful=0.6635`, but this is not evidence that epochs 9-16 help; validation usefulness degraded after epoch 5 and score scale exploded (`pred_std=192.46` by epoch 16).
- Half-LR extended run `range_aware_t25_e16_pool4_lr25_20260513` used LR `0.00025`. It was more stable and picked epoch 16, but final eval was lower: `RangeUseful=0.6604`. It mostly showed slower convergence, not a better model.
- Current epoch conclusion: default LR reaches diminishing returns around epochs 5-6. Lower LR delays instability but does not improve final usefulness. Increasing epochs alone is the wrong lever; the next likely levers are LR decay after epoch 4-5, score-scale regularization, or cheaper dense checkpoint selection.
- Ran coverage/compression sweep for `query_coverage` targets `0.10`, `0.20`, and `0.40`, each audited at compression ratios `0.01`, `0.02`, `0.05`, and `0.10`. Results are summarized in `Aleks-Sprint/range-aware-coverage-compression-sweep-20260513.md`.
- The model beat uniform, Douglas-Peucker, and TemporalRandomFill in all 12 tested coverage/compression cells. At 20% coverage, `RangeUseful` was `0.3999/0.5047/0.6626/0.7859` for `1%/2%/5%/10%` compression.
- Coverage sensitivity is real. At 5% compression, `RangeUseful` was `0.7388` for 10% query coverage, `0.6626` for 20%, and `0.5533` for 40%. Higher coverage is less selective, lowers the oracle ceiling, and shrinks the model margin.
- Runtime sensitivity is also real. Coverage 40% produced about 1.1k queries per split and took `4431s`; train label prep alone took `940s`, eval label prep `829s`, range diagnostics `876s`, and MLQDS 5% eval latency was `133s`. High-coverage sweeps need label/diagnostic optimization or aggressive cache warming.
