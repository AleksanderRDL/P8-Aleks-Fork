# Archived Artifacts

Historical QDS reports retained for context. These files are not current
operating documentation and should not be used as benchmark instructions.

Use [`../README.md`](../README.md) and the module READMEs under
[`../src/`](../src/) for current commands, artifact layout, evaluation semantics,
and benchmark workflow.
