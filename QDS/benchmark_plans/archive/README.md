# Archived Benchmark Plans

These plans are historical or diagnostic. They are not active redesign plans.

`range_aware_coverage_compression_grid.tsv` runs the old workload-aware
`range_aware` profile across the requested coverage/compression grid. It can be
used as a teacher/upper-bound diagnostic, but it is not a workload-blind
benchmark and must not be used as final success evidence.
