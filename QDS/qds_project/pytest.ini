[pytest]
markers =
    unit: Fast, isolated tests (default for unclassified tests).
    integration: Cross-module tests that validate orchestration and component interaction.
    slow: Higher-cost tests (for example model training paths), suitable for less frequent runs.
