# Single repository dependency entry point.
# Keep this file to direct project dependencies. Transitive packages should be
# installed through their parent dependency unless there is a concrete reason
# to pin them here.

# Runtime
numpy==2.4.4
pandas==3.0.3
psycopg[binary]==3.3.4
pyarrow==24.0.0
pyspark==4.1.1
python-dotenv==1.2.2
torch==2.12.0

# Dev/test
pyright==1.1.409
pytest==9.0.3
